import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'red',
    },
    content: {
        backgroundColor: 'yellow',
        width: '100%',
        height: '100%',
    },
});
