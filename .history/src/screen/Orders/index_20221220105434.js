import React from 'react';
import { ScrollView, SafeArray, Text } from 'react-native';

export default function Orders() {
    return (
        <ScrollView>
            <SafeArray>
                <Text> Orders</Text>
            </SafeArray>
        </ScrollView>
    );
}
