import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginTop: 100,
        alignSelf: 'center',
        flexDirection: 'row',
    },
    imageIcon: {
        width: 50,
        height: 50,
    },
});
